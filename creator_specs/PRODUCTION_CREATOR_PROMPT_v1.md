# Production Creator Prompt — v1

**🔒 FROZEN PRODUCTION VERSION — IMMUTABLE. Codex-approved (3 rounds for §0-§6; 3 rounds for the freeze action itself). Do not edit this file directly. Any future rule change must go through the Analytics → Review → Prompt Update workflow and land in a new version (v2+), not as an edit to this file. See `VERSION_MANIFEST_v1.md` for build provenance and integrity hashes.**

**Status: rule content (§0-§6) Codex-approved, 3 rounds, before freezing — the freeze action (this banner) has not itself been applied to any production file (`content_categories.py` or any generator). This prompt has not been used to generate any script or content. See `VERSION_MANIFEST_v1.md` for the separate content-only integrity hash that lets a future reviewer verify §0-§6 was never touched by the freeze edit.**

**Declared normative merge inputs (corrected per Codex review — reworded from a process claim to a verifiable content claim):** this document's rule content (§0-§6) is built from exactly `content_repo_clone/CORE_OS/BRAND_BIBLE.md` (the Brand Manifesto), `creator_specs/CREATOR_SPECIFICATION_v2.md` (the Creator Specification), and `creator_specs/PROMPT_DELTA_v1.md` (the Codex-approved Prompt Delta, v1.4) — every rule/section below cites which of these three it came from. No content in §0-§6 is directly attributed to the Pipeline Specification, any analytics/audit report, or the candidate knowledge package. Where the Creator Specification or Prompt Delta themselves cite one of those other documents (e.g. `CR-1`'s "Source" field, or a delta's "Evidence" field), that citation is reproduced as metadata already present inside a permitted source file, not as new content pulled from the cited document.

**Status note on `CR-1`, corrected per Codex round 1 — no redefinition of "ACTIVE," stated plainly:** by the Creator Specification's own, unmodified definition ("currently enforced in a production prompt/rubric today"), **no Creator Rule is currently ACTIVE** — `CR-1` remains `EXPERIMENTAL`, exactly as tagged in the Creator Specification. Building this document does not change that status; `CR-1` becomes `ACTIVE` only once its text is actually enforced inside a generator's live judge prompt — per the Creator Specification's definition, that means the text must be executing as part of a real request to the model, not merely present in a file. (This distinction matters concretely here: the Prompt Delta's own investigation found `content_categories.py` entries can exist without being wired into any generator's live prompt via `category_rubric_block()` — presence in a file is not sufficient evidence of enforcement.) Nothing in this document has reached that bar. `CR-1` is included in this candidate document because the task instruction required merging the Brand Manifesto, the Creator Specification, and the Approved Prompt Delta — and the Approved Prompt Delta exists specifically to implement `CR-1`. Its inclusion here reflects that merge instruction, not a claim that `CR-1` has met the bar for `ACTIVE`.

---

## §0. Brand Manifesto (preserved exactly, verbatim, unmodified)

> # Core Brand System
>
> This file defines reusable brand-system mechanics only: tone governance, trust standards, audience safety, asset lineage, and domain routing. Domain-specific brand expression lives inside each domain manifest, guide, series bible, and creative knowledge asset.
>
> Core brand rules: be accurate, humane, evidence-aware, non-manipulative, and clear about uncertainty. Domain rules may be stricter and override generic phrasing within their scope.

*(Source: `content_repo_clone/CORE_OS/BRAND_BIBLE.md`, reproduced in full. Not summarized, not paraphrased, not edited.)*

---

## §1. Universal Content Rule — Uncertainty Hedging

> Any script sentence that (a) states a future outcome as certain, or (b) presents a belief/tradition-based claim as flat fact, must use hedge language appropriate to the domain (e.g. "có thể," "thường," "theo quan niệm," "theo truyền thống") rather than unqualified certainty.

This rule operationalizes §0's Brand Manifesto line — *"clear about uncertainty"* — into a checkable judge-panel criterion. It does not alter the Manifesto; the Manifesto text in §0 is unmodified.

**Two independent branches — a script sentence can pass one and still fail the other:**
1. **Certainty branch:** a future outcome/result stated as certain (e.g. "chắc chắn," "vô cùng," "tuyệt đối," "hứa hẹn").
2. **Belief-framing branch:** a belief/tradition-based claim (phong thủy/tử vi/lịch pháp relationships) presented as flat objective fact, without hedge/attribution language ("theo quan niệm," "được xem là," "có thể," "thường").

**Applied below per category**, since the concrete check differs by generator/rubric baseline.

---

## §2. Category 1 — Daily Grounded Data

Applies to 6 generators, each with its own hand-written judge prompt (this category's shared rubric constant in `content_categories.py` is not wired into any of these 6 files — each generator's `BƯỚC 1` below must be edited individually).

### §2.1 `element_color_short_generator.py`

```
BƯỚC 1 -- FACT-CHECK (LOẠI TRỪ TRƯỚC): với MỖI phương án, kiểm tra: (a) CHỈ dùng đúng màu/tên mệnh có trong dữ liệu, không thêm màu khác, (b) không cam kết kết quả chắc chắn dưới bất kỳ hình thức nào -- không chỉ "giàu có/may mắn tuyệt đối" mà cả các khẳng định nhân quả không dè dặt (vd "gây khắc chế và làm năng lượng mất cân bằng" nói như sự thật tuyệt đối), (c) không bịa vật phẩm cụ thể ngoài ví/hình nền/trang phục, (d) có trình bày mối quan hệ Ngũ Hành (khắc chế, tương sinh, bổ trợ...) như SỰ THẬT KHÁCH QUAN, không có framing dè dặt ("theo quan niệm Ngũ Hành", "được xem là", "có thể") không? Đây là điểm ĐỘC LẬP với (b) -- 1 câu có thể không "cam kết chắc chắn" (không dùng từ tuyệt đối) nhưng VẪN vi phạm nếu trình bày quan hệ Ngũ Hành như sự thật khoa học. Có thì FAIL. Vi phạm bất kỳ điểm nào → LOẠI.
```

### §2.2 `element_luck_short_generator.py`

```
BƯỚC 1 -- FACT-CHECK (LOẠI TRỪ TRƯỚC): với MỖI phương án, kiểm tra: (a) CHỈ dùng đúng tên mệnh/màu trong dữ liệu, (b) không bịa lý do/sự kiện cụ thể, (c) không cam kết kết quả chắc chắn, (d) TUYỆT ĐỐI không nhắc "Hoàng Đạo"/"Hắc Đạo"/ngày tốt xấu tổng thể, (e) LỖI THẬT ĐÃ GẶP -- có câu nào viết kiểu "Ngày [day_can_chi] thuộc hành [day_element]" không (vd "Canh Tý thuộc hành Kim")? Đây là SAI vì day_element chỉ là hành của riêng Thiên Can, không phải cả tổ hợp Can Chi -- có thì FAIL ngay, (f) có trình bày mối quan hệ mệnh/ngũ hành/can chi như SỰ THẬT KHÁCH QUAN mà không có framing dè dặt phù hợp ("theo quan niệm", "được xem là") không? Đây là điểm ĐỘC LẬP với (c) -- 1 câu có thể pass (c) (không dùng từ chắc chắn/tuyệt đối) nhưng vẫn FAIL ở đây nếu thiếu framing dè dặt cho 1 nhận định dựa trên niềm tin truyền thống. Vi phạm bất kỳ điểm nào → LOẠI.
```

### §2.3 `twelve_gods_short_generator.py`

```
BƯỚC 1 -- FACT-CHECK (LOẠI TRỪ TRƯỚC): với MỖI phương án, kiểm tra: (a) tên Thần đúng god_name không, (b) loại tốt/xấu đúng god_status không, (c) KHÔNG bịa thêm truyền thuyết/quyền năng/hậu quả cụ thể ngoài god_description, (d) nếu god_status=bad thì giọng văn có "doạ dẫm hậu quả nghiêm trọng cụ thể" không (vd nói thẳng "sẽ mất tiền/gặp tai nạn") -- có thì FAIL vì vi phạm quy tắc không mê tín tuyệt đối, (e) có LIỆT KÊ từ 2 giờ trở lên liền nhau không (vd "Sửu, Thìn, Ngọ, Mùi...") -- có thì FAIL vì dông dài/khó nghe, (f) (áp dụng cho CẢ god_status=good VÀ bad, độc lập với check doạ dẫm ở (d)): có dùng ngôn ngữ khẳng định chắc chắn cho kết quả (vd "chắc chắn", "tuyệt đối", "vô cùng may mắn") KHÔNG, và có trình bày quyền năng/ảnh hưởng của vị Thần như sự thật khách quan mà không có framing dè dặt ("theo tín ngưỡng dân gian", "được cho là") không? Vi phạm 1 trong 2 → FAIL. Phương án nào vi phạm bất kỳ điểm nào → LOẠI.
```

### §2.4 `zodiac_month_short_generator.py`

```
BƯỚC 1 -- FACT-CHECK (LOẠI TRỪ TRƯỚC): với MỖI phương án, kiểm tra: (a) CHỈ nhắc đúng con giáp trong cautioned_animals, (b) không bịa lý do cụ thể, (c) không doạ dẫm hậu quả, (d) TUYỆT ĐỐI không nhắc "Hoàng Đạo"/"Hắc Đạo", (e) có nói rõ đây là THEO THÁNG (không phải theo ngày) không, (f) (độc lập với check doạ dẫm ở (c)): có dùng ngôn ngữ khẳng định chắc chắn/tuyệt đối cho hậu quả/kết quả không, và có trình bày quan hệ xung khắc/tương hợp theo tháng như sự thật khách quan mà không có framing dè dặt ("theo quan niệm", "được xem là") không? Vi phạm 1 trong 2 → FAIL. Vi phạm bất kỳ điểm nào → LOẠI.
```

### §2.5 `lich_hoang_dao_generator.py`

```
BƯỚC 1 -- FACT-CHECK (LÀM TRƯỚC, LOẠI TRỪ): với MỖI phương án, đối chiếu TỪNG con số/tên gọi (ngày âm lịch, Can Chi, tên Trực, Hoàng Đạo/Hắc Đạo, giờ tốt, hướng) với dữ liệu gốc. Phương án nào có BẤT KỲ sai lệch/tự thêm chi tiết không có trong dữ liệu gốc → LOẠI, không được chọn làm winner dù hook hay đến đâu. Cũng kiểm tra: có đánh dấu ** hợp lý không (1-3 cụm/câu, đúng phần mang thông tin) hay đánh dấu tràn lan/thiếu? RIÊNG PHẦN GIỜ/HƯỚNG: phương án nào LIỆT KÊ từ 2 giờ/hướng trở lên liền nhau trong 1 câu (vd "giờ Dần, Thìn, Tỵ..." hoặc nhiều cặp ngoặc đơn thời gian liên tiếp) → LOẠI, vì nghe qua giọng đọc dông dài/khó theo dõi -- chỉ chấp nhận phương án chọn ĐÚNG 1 giờ/1 hướng nổi bật. Cũng kiểm tra: có dùng ngôn ngữ khẳng định chắc chắn/tuyệt đối cho mức độ tốt xấu của ngày/giờ/hướng không (vd "chắc chắn đại cát", "tuyệt đối thuận lợi"), VÀ có trình bày ý nghĩa cát/hung của ngày như sự thật khách quan mà không có framing dè dặt ("theo lịch pháp truyền thống", "được xem là") không? Vi phạm 1 trong 2 → FAIL, cùng mức nghiêm trọng như sai lệch dữ liệu.
```

### §2.6 `zodiac_short_generator.py`

```
BƯỚC 1 -- FACT-CHECK (LOẠI TRỪ TRƯỚC): với MỖI phương án, kiểm tra: (a) CHỈ nhắc đúng các con giáp trong hop_animals/ky_animal, không thêm con giáp khác, (b) không tự gán lý do/vận may cụ thể (tài lộc, tình duyên, sức khoẻ...) không có trong dữ liệu, (c) với ky_animal không doạ dẫm hậu quả cụ thể, (d) LỖI THẬT ĐÃ GẶP -- có dùng cụm tăng cường mức độ chắc chắn không (vô cùng, đại cát, chắc chắn, tuyệt đối, hứa hẹn...)? Dữ liệu chỉ xác nhận quan hệ Tam Hợp, không chứng minh mức độ thuận lợi cụ thể -- có thì FAIL, (e) có trình bày quan hệ Tam Hợp/Tứ Hành Xung hoặc mức độ hợp/kỵ như SỰ THẬT KHÁCH QUAN mà không có framing dè dặt ("theo quan niệm dân gian", "được xem là") không? Đây là điểm ĐỘC LẬP với (d) -- 1 câu có thể pass (d) (không dùng từ cường điệu) nhưng vẫn FAIL ở đây nếu thiếu framing dè dặt. Vi phạm bất kỳ điểm nào → LOẠI.
```

*(Open item carried from the Prompt Delta, not resolved here: `zodiac_short_generator.py`'s existing criterion (d) already named the exact phrases found in a confirmed violation, yet that video still published with those phrases. Cause unconfirmed — no version history exists for this file. Not a blocker for adopting the merged text above.)*

---

## §3. Category 4 — Creative Astrology

Applies to `western_zodiac_short_generator.py`, via `content_categories.py`'s shared `CATEGORY_RUBRICS[CREATIVE_ASTROLOGY]` (confirmed: exactly one generator uses this category, and it correctly wires in the shared rubric).

```
TIÊU CHUẨN CATEGORY 4 -- CREATIVE ASTROLOGY (đánh giá sáng tạo, KHÔNG fact-check dữ liệu):
- KHÔNG áp dụng fact-check kiểu đối chiếu số liệu -- thể loại này (cung hoàng đạo, tính cách, tình yêu...) vốn không có "đúng-sai" tính toán được.
- Đánh giá theo CHẤT LƯỢNG SÁNG TẠO: có sáo rỗng/chung chung không (áp dụng cho cung nào cũng đúng = sáo rỗng, LOẠI), có tuyệt đối hoá không (nói chắc chắn điều sẽ xảy ra = LOẠI), có giọng văn hấp dẫn/cụ thể cho ĐÚNG cung đang nói không.
- Đây là nội dung GIẢI TRÍ CÓ TRÁCH NHIỆM -- không khẳng định chắc chắn tương lai, nhưng vẫn được vui/hấp dẫn/cụ thể.
- ĐỘC LẬP với việc không "tuyệt đối hoá tương lai": mỗi khẳng định về đặc điểm tính cách/nguyên tố PHẢI dùng framing chủ quan/xu hướng phù hợp cho nội dung chiêm tinh giải trí (vd "thường/hay có xu hướng/dễ...") thay vì khẳng định trực tiếp như sự thật khách quan về cung đó. Phương án nào có câu khẳng định trực tiếp đặc điểm tính cách/nguyên tố mà KHÔNG dùng framing này → LOẠI, độc lập với việc có "tuyệt đối hoá tương lai" hay không.
```

**Disclosed design tension (carried from the Prompt Delta, not resolved here):** this category's own design explicitly rejects fact-checking and rewards a confident, vivid voice as a positive quality. The 4th bullet above applies `CR-1` uniformly, as approved — but hedged personality writing may read weaker than this category's current confident style, and that confident style may be an intentional part of what makes the category's content engaging, not a defect. This tension was surfaced during review and is preserved here rather than resolved by quietly softening the rule.

---

## §4. Category 2 — Educational Knowledge

Applies to `educational_short_generator.py`, via `content_categories.py`'s shared `CATEGORY_RUBRICS[EDUCATIONAL]` (confirmed: exactly one generator uses this category).

```
TIÊU CHUẨN CATEGORY 2 -- EDUCATIONAL KNOWLEDGE (đúng tài liệu, được kể chuyện):
- Nội dung phải ĐÚNG theo tài liệu tham khảo (facts/excerpt) -- không phát minh kiến thức mới, không diễn giải sai lệch ý gốc.
- ĐƯỢC PHÉP giải thích, kể chuyện, minh hoạ bằng ví dụ, so sánh -- miễn là không mâu thuẫn với tài liệu gốc. KHÔNG cần tạo "dữ liệu của hôm nay" -- đây là kiến thức nền, không gắn ngày.
- Mục tiêu là GIÁO DỤC dễ hiểu, không phải liệt kê khô khan.
- Nếu nội dung trình bày một hệ thống/khái niệm truyền thống (Ngũ Hành, Âm Dương, Can Chi...) như cơ chế nhân quả, MỖI câu trình bày một quan hệ/cơ chế cụ thể trong hệ thống đó (vd "X khắc Y", "X sinh Y") PHẢI tự mang framing truyền thống trong chính câu đó hoặc trong mệnh đề ngữ pháp trực tiếp bao trùm nó (vd 1 câu dẫn nhập "Theo hệ thống Ngũ Hành truyền thống, chu trình này gồm:" ngay trước một danh sách liệt kê các quan hệ, nếu mối liên hệ ngữ pháp giữa câu dẫn và danh sách là rõ ràng và liên tục -- KHÔNG chấp nhận 1 câu hedge ở đầu kịch bản rồi các câu sau, không liên quan ngữ pháp trực tiếp, trình bày cơ chế như sự thật khách quan). KHÔNG được trình bày cơ chế này như quy luật vật lý đã được chứng minh khách quan.
```

**Note:** this category's judge prompt (in `educational_short_generator.py` itself, not the shared rubric) already has a separate certainty-branch check ("tuyệt đối hoá quá mức → LOẠI"). The 4th bullet above adds only the belief-framing branch — it does not duplicate the certainty check.

---

## §5. Category 3 — Traditional Interpretation

Applies to `iching_short_generator.py`, via `content_categories.py`'s shared `CATEGORY_RUBRICS[INTERPRETATION]` (confirmed: exactly one generator uses this category).

```
TIÊU CHUẨN CATEGORY 3 -- TRADITIONAL INTERPRETATION (được diễn giải, không phải chân lý tuyệt đối):
- ĐƯỢC PHÉP diễn giải/ứng dụng truyền thống (vd giải nghĩa quẻ, biểu tượng văn hoá) -- đây là nội dung diễn giải, KHÔNG phải dữ liệu tính toán được như Category 1.
- BẮT BUỘC phải nói rõ đây là "1 cách hiểu"/"1 cách ứng dụng truyền thống" -- KHÔNG được trình bày như chân lý tuyệt đối duy nhất, KHÔNG được tuyên bố dự đoán chính xác tương lai của người xem cụ thể. Áp dụng yêu cầu framing này cho MỌI câu có nội dung diễn giải/gán ý nghĩa, kể cả câu ngắn chỉ nêu biểu tượng/tên quẻ (vd "Đó là biểu tượng của quẻ X" cũng cần framing truyền thống nếu đứng như 1 khẳng định độc lập, không chỉ các câu diễn giải dài).
- Vẫn phải đúng KHUNG truyền thống thật (tên quẻ, ý nghĩa cổ điển...) -- không bịa khái niệm không tồn tại, chỉ được diễn giải MỞ trong khuôn khổ khái niệm có thật.
```

---

## §6. No rule this version

- **Hook, Story structure, Pacing, Typography, CTA (all categories):** no approved finding exists in the Creator Specification for these areas. Existing generator content in these dimensions is unmodified by this document.
- **Category 5 — Storytelling (`storytelling_short_generator.py`):** no delta proposed; existing prompt unmodified.
- **Brand-specific channels (Buddhist, Crime, other):** no new rule from the Creator Specification; unmodified.

---

## Prompt Changelog

| Change | Source | Note |
|---|---|---|
| §0 Brand Manifesto added, verbatim | `content_repo_clone/CORE_OS/BRAND_BIBLE.md` | Unmodified; reproduced exactly per requirement |
| §1 Universal rule statement added | Creator Specification `CR-1` | Normative statement, two branches |
| §2.1-§2.6 — 6 generator judge-prompt merges (baseline + delta) | Prompt Delta Deltas 1-6 | Each merge shown in full, ready to paste into the named file's `_JUDGE_PROMPT` |
| §3 Creative Astrology rubric merge | Prompt Delta Delta 7 | Design tension disclosed, not resolved |
| §4 Educational rubric merge | Prompt Delta Delta 8 | Belief-framing branch only; certainty branch already exists elsewhere in that generator's own prompt |
| §5 Interpretation rubric merge | Prompt Delta Delta 9 | Small clarifying addition to an existing bullet, not a new bullet |
| §6 explicit no-rule scope note | Creator Specification §1.1-1.5, §2 | Prevents the document from silently implying coverage it doesn't have |

**Status note (round 1-2 fixes):** `CR-1` remains tagged `EXPERIMENTAL` in the Creator Specification and in this candidate document — merging it here does not change that status. It becomes `ACTIVE` only once its text is actually enforced inside a generator's live judge prompt (not merely present in a file — see the note in the document header for why that distinction is load-bearing here). **This document has not been used to generate any script or content.**

**This is v1 — no prior Production Creator Prompt exists.**

---

## Rule Mapping — Creator Rule → Prompt section

| Creator Rule | Prompt section(s) |
|---|---|
| `CR-1` (Uncertainty-hedging) | §1 (normative statement); §2.1-§2.6 (Grounded Data, 6 generators); §3 (Creative Astrology); §4 (Educational); §5 (Interpretation) |
| *(no other Creator Rules exist in the Creator Specification)* | — |

---

**Awaiting approval.** Nothing in §0-§6 has been applied to `content_categories.py` or any generator file, and this prompt has not been used to generate any script or content.
